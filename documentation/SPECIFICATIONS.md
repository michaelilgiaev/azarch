# Az'arch -- Technical Specification

- Auto-generated by `scripts/pull_specifications.sh`. Do not hand-edit; re-run to refresh.
- Generated: 2026-07-19 15:51:21 UTC
- Host: Linux 6.12.91-1-MANJARO (x86_64)

---

## Contents
- 1. Repository -- identity, git state, directory scheme
- 2. Build system (Python) -- module hierarchy, config-as-Python, declared ISO identity
- 3. Package manifest -- the ISO's software set by category
- 4. Build toolchain -- declared (Docker) vs detected (this host)
- 5. Build artifacts -- resolved on-disk state (cache, ISOs, logs)

---

## 1. Repository
- **Name:** azarch (Az'arch Linux) -- Python-driven Arch ISO builder
- **Root:** `/home/main/Repositories/azarch`
- **Git branch:** `master`
- **Git HEAD:** `bd4e954` -- Add Plasma X11 session entry and post-pacstrap branding hook
- **Working tree:** dirty (uncommitted changes)
- **License:** `LICENSE.txt` present (714 lines)
- **Readme:** `README.md` present (209 lines)

### 1.1 Directory scheme
- The build writes everything under a fixed set of top-level directories.
- `cache/`, `output/`, and `logs/` are git-ignored and are the three Docker bind-mount points.

```text
libraries/azarch/        296K   the build system itself (config-as-Python + build logic)
libraries/data/           28K   verbatim upstream files: packages.x86_64, big KDE QML
assets/                  2.2M   brand assets: logos, fastfetch ASCII art
scripts/                  56K   auxiliary tooling (this spec generator)
documentation/           4.0K   human-facing docs (incl. the generated spec)
cache/                    25G   [gen] persistent download cache: pkgs, DBs, disposable build/ tree
output/                  6.0G   [gen] the finished .iso lands here
logs/                    288K   [gen] full.log + steps.log
```

### 1.2 Source vs. generated
- **Source of truth:** `libraries/azarch/` (Python) + `libraries/data/` (verbatim data).
- **Generated at build time:** the archiso profile tree under `cache/build/`, the ISO in `output/`, logs in `logs/`. None of it is committed.
- **This report:** `scripts/pull_specifications.sh` -> `documentation/SPECIFICATIONS.md`.

---

## 2. Build system (Python)
- **Package:** `azarch` (import root: `libraries/`, set as PYTHONPATH by compile.sh)
- **Entrypoint:** `python3 -m azarch.build` (invoked by the `compile.sh` PTY/sudo shim)
- **Design:** config-as-Python: every ISO artifact is a Python string/function, emitted into the archiso profile tree
- **python3 (this host):** 3.14.6

### 2.1 Core modules -- `libraries/azarch/`
- Each line is the module's own one-line docstring summary (read from source).

```text
paths          Filesystem layout for the build.
emit           Emit contract: write config-as-Python content out as real files in the ISO tree.
progress       Weighted, pinned-to-bottom progress bar.
ownership      Never leave cache/ output/ logs/ root-owned and locked on the host.
packages       Real-time, resumable package cache -- the port of the old cache-pkgs.sh.
steps          The ordered build steps: assemble the archiso profile tree from the
build          Build entrypoint: `python3 -m azarch.build`.
```

### 2.2 Config-as-Python -- `libraries/azarch/config/`
- One module per class of ISO artifact. Content lives here as variables/builders; `steps.py` places it.

```text
pacman         All pacman.conf variants, generated in Python.
system         System-level configs: users, sudoers, display manager, session, boot menus,
locale         Locale auto-detection: the country->locale map (as a Python dict, the single
kde            The small, hand-tuned KDE config files, as Python strings.
installer      The on-disk install pipeline scripts, authored in Python and emitted as the
profile        profiledef.sh -- the archiso profile definition mkarchiso sources.
fastfetch      fastfetch config + the azarch "Az'" ASCII logo.
```

### 2.3 Declared ISO identity & layout
- **iso_name:** `azarch`
- **iso_application:** Az'arch Installer/Az'arch Linux Live/Rescue DVD
- **iso_publisher:** michaelilgiaev <https://github.com/michaelilgiaev/azarch>
- **install_dir:** `arch`
- **iso_version scheme:** date-based `%Y.%m.%d` (from SOURCE_DATE_EPOCH at build time)
- **boot modes:** 6 (BIOS syslinux + UEFI ia32/x64 systemd-boot, esp+eltorito)
- **file_permissions entries:** 13 (locked shadow/gshadow/sudoers/azarch payload in the squashfs)
- **airootfs image:** squashfs, zstd -Xcompression-level 15 (xz error-9 workaround)

### 2.4 Declared users (from `config/system.py`)
```text
LOGIN        UID    GID    SHELL
root         0      0      /usr/bin/bash
main         1000   998    /usr/bin/bash
```
- (Parsed from the embedded `/etc/passwd` string; the live ISO also autologins via SDDM.)

---

## 3. Package manifest
- **File:** `libraries/data/packages.x86_64`
- **Total packages:** 243
- **Architecture:** x86_64

### 3.1 Composition by category
- Heuristic binning by package name (for a human overview, not a dependency graph).

```text
base / kernel / boot                18
KDE Plasma desktop                  66
graphics / display stack             8
networking                          37
storage / filesystems / install     38
dev toolchain                       12
multimedia / office / a11y          16
hardware / peripherals / VM         17
CLI / shell / utilities             31
TOTAL                              243
```

### 3.2 Toolchain & desktop highlights
- **Kernel:** `linux` (present)
- **Desktop meta:** `plasma` (present)
- **Plasma desktop:** `plasma-desktop` (present)
- **Display manager:** not in manifest
- **Network stack:** `networkmanager` (present)
- **Audio:** `pipewire` (present)
- **Python:** `python` (present)
- **Go:** `go` (present)
- **.NET SDK:** `dotnet-sdk` (present)
- **Git:** `git` (present)
- **Editor (nvim):** `neovim` (present)
- **Fetch tool:** `fastfetch` (present)
- **Office suite:** `libreoffice-fresh` (present)
- **Media player:** `vlc` (present)
- **Installer (upstream):** `archinstall` (present)
- _Note:_ `sddm` is not an explicit manifest entry but is pulled in by the `plasma` group; the build configures SDDM autologin regardless.

---

## 4. Build toolchain
### 4.1 Declared build environment (Dockerfile)
- **Base image:** `archlinux:latest`
- **Rationale:** mkarchiso resolves the ISO package list against the host's Arch repos; a genuine Arch userland is mandatory

- Packages the image installs for the build:

```text
archiso            provides mkarchiso (the ISO assembler)
base-devel         makepkg and the C/build toolchain
go                 builds Go-based ISO components
git                checkout tooling
sudo               the build shells out through sudo internally
python             the build itself (python3 -m azarch.build)
archlinux-keyring  refreshes signing keys before install
```

- **Trust setup:** `pacman-key --init && --populate archlinux` (required or signed pkgs are rejected)
- **PID 1:** tini via `docker run --init` (signal forwarding + orphan reaping for clean Ctrl-C)
- **Privileges:** `--privileged` (mkarchiso mounts proc/sys/dev, uses loop devices + squashfs)

### 4.2 Detected on this host
- Versions present on the machine generating this report (may differ from the Docker environment).

- **archiso (mkarchiso):** present -- archiso 88-1
- **pacman:** Pacman v7.1.0 - libalpm v16.0.1
- **python3:** Python 3.14.6
- **go:** go version go1.26.5-X:nodwarf5 linux/amd64
- **git:** git version 2.55.0
- **docker:** Docker version 29.5.1, build 2518b52d94
- **sudo:** Sudo version 1.9.17p2

- **archiso releng profile:** present at `/usr/share/archiso/configs/releng` (native builds copy from here)

---

## 5. Build artifacts (resolved state)
- **Combined artifact size:** 31G

### 5.1 Package cache -- `cache/`
- **pacstrap CacheDir (`cache/pacman-pkg/`):** 1208 cached packages, 2.6G
- **Offline local repo (`cache/pkgs/repo/`):** 1208 packages, 2.6G
- **Local repo index:** `pacstrap-azarch-repo.db` present -> offline rebuilds enabled
- **Synced pacman DBs (`cache/pkgs/db/sync/`):** 3 database(s)
- **Scratch build tree (`cache/build/`):** present, 20G (disposable mkarchiso workdir)

- **Offline-ready:** cache is complete; a rebuild will contact no server (matches `cache_is_complete()`).

### 5.2 Output -- `output/`
- **ISO:** `azarch-2026.07.19-x86_64.iso` -- 6.0G

### 5.3 Logs -- `logs/`
- **full.log:** 284K (5837 lines)
- **steps.log:** 4.0K (2 lines)
- Last milestone: `[ 25/25 ] [OK] ISO built successfully: output/azarch-2026.07.19-x86_64.iso (6.0G)`

---

## How this was produced
- Entry point: `scripts/pull_specifications.sh` (this script).
- Section modules: `scripts/libraries/{common,probe_repo,probe_python,probe_packages,probe_toolchain,probe_artifacts}.sh`.
- Read-only: the generator inspects the repo and host; it never mutates the build.
- Values such as the ISO identity, user table, and toolchain list are read from source, so this doc tracks the code.
